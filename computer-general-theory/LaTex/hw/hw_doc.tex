\documentclass[headings=big]{scrreprt}
% article: section的序号是1
% scrreprt: section序号是0.1，行中的内容较少，字体不同
    %       chapter的序号是1
    %  chapterfix显示大大的chapter1
    % headings=small和big的之间头的间隔不同,头的大小不同
    % numbers=enddot  数字后面有·
\usepackage[T1]{fontenc}

\begin{document}
\chapter{Introduction}
This a sample document with some dummy text \footnote{See here}
This is a sample document with some dummy
text\footnote{and a footnote}. This paragraph is quite
long as we might want to see the effect of making the
document have two columns.
This is a sample document with some dummy
text\footnote{and a footnote}. This paragraph is quite
long as we might want to see the effect of making the
document have two columns.
\end{document}