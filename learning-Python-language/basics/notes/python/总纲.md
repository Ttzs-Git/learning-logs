# Python

## 抽象

- 赋值(assignment):将名称和一个值或者函数进行绑定，保留值或者函数逻辑
- 导入:import 语句-将一个built-in的函数或者变量和进行绑定
- 定义:def 语句-与变量保持实时更新

## 元素

### first-class status

1. 可以与名称绑定
2. 可以作为参数传递给函数
3. 可以作为函数的结果返回
4. 可以包含在数据结构中

