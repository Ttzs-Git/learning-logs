## 表达式

- 表达式描述一个计算并评估为一个值

### 表达式的结构

```python
operator(operand,operand...)
```

### 表达式的求值规则

1. 按顺序评估运算符，然后评估操作数子表达式
2. 从运算符中得到一个函数,从操作数中获取参数
3. 将该运算符的值作为应用于操作数的值

> 1. 计算子表达式
> 2. 应用函数

### 嵌套表达式

- 表达式树

