## 运算符

### 列表运算符([] [:])

### 为运算符(~ >> << & ^ |)

### 算数运算符( + - * / // % **)

### 比较运算符(== != <  >  <=  =>| 返回布尔值)

### 身份运算符(is  is not)

### 成员运算符(in not in)

### 逻辑运算符(and or not| 返回布尔值|短路处理)

### 赋值运算符(= += -= *= /= %= //= &/ |= ^= >>= <<=)