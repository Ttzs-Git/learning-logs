## 函数总纲

### 函数的分类

- **纯函数函数**[有一些输入（参数）并返回一些输出(调用返回结果)]

- **非纯函数**[拥有副作用]

####  抽象函数的属性

- **域（Domain）**：函数可以接受的所有有效输入集合。
- **范围（Range）**：函数能够输出的所有可能结果集合。
- **意图（Intent）**：描述了函数设计的目的，即它如何将输入转换为输出（也可能包括副作用）。

